function startVideoCall() {
            if (!window.currentRoom) {
                showToast && showToast('Bạn chưa vào phòng học. Hãy tham gia lớp trước!', 3000);
                return;
            }
            const btn = document.getElementById('start-video-btn');
            try {
                if (isVideoCallActive) {
                    await window.currentRoom.localParticipant.setCameraEnabled(false);
                    await window.currentRoom.localParticipant.setMicrophoneEnabled(f